(ns com.anonimito.barebones-clojure-test
  (:require [clojure.test :refer :all]
            [com.anonimito.barebones-clojure :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
